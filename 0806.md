#   어떤 문제가 있었는지
#   내가 시도해본 것들
#   어떻게 해결했는지
#   무엇을 새롭게 알았는지
    #Python 3.14.. 
     But 특정 소스는 3.14버전 호환X ㅡ> 가상환경 구축

    #	구축방식
        1. Pyton -m venu 
        2. 아나콘다
        ㅡ> 1,2번은 느리고 의존성 문제가 자주 발생
        3. uv
            1) Rust 기반이라 빠르다
            2) 프로젝트 단위 관리
            3) 의존성 궁합 잘 맞다

    #	 검색창에 powerwhell 검색


    #	 버전 0. 12. 2
        0: major 버전 ㅡ> 하위 버전과 호환X
        12: minor 버전 ㅡ> 하위 버전과 호환되고 기능 추가등 작은 변경
        (파이썬은 호환이 잘안되기도함)
        2: Patch 버전 ㅡ> 오류수정, 코드개선

    #	 uv 프로젝트 생성+가상환경
         0. uv init --python 3.13  =>python 버전은 생략해도됨
         pyproject.toml ㅡ>이걸 공유하면 동일한 가상 환경을 만들 수 있음
        1. uv 설치: powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex" 
        2. PATH 설정: set PATH=%PATH%;C:\Users\o_o__\.local\bin 
            uv 버전 확인: uv --version 
        3. 프로젝트 생성 : uv init --python 3.13 
        ㅡ> 1,2,3번은 최초 1회만
        4. 판다 설치: uv add pandas  ㅡ> 의존성을 추가할때 uv add를 사용함
        5. 가상환경 생성, 동기화: Uv sync ㅡ>toml 의존성에 맞춰 .venv를 생성, 동기화
        6. 가상환경 활성화: .venv\Scripts\activate 
        7. 가상환경 비활성화: .venv\Scripts\deactivate.bat
        8. 파이썬 버전 고정: uv python pin 3.12
        9. 코드 출력: uv run main.py

    	파이썬 실행: python
        파이썬 나가기: exit()

    # 변수: 값을 가진 공간의 주소를 가진 이름표
    # 변수명(=값) 명명 규칙
      영문(대소문자), 숫자,$,_로 작성
      숫자는 앞에X, 공백은 포함X
      예약어X (if, for,True)

    # 변수명 권장 사항
      의미있는단어
      여러단어 구성은 _로 구분

    # 자료->연산에 사용되는 값
    # 자료형: 자료의 종류
    # 숫자형
